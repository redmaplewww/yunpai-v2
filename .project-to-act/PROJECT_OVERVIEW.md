# 项目总览

> 当前项目方向与边界的紧凑视图。详细设计和过程材料保存在项目正常文档中，此处只记录短摘要与引用。

## 基本信息

- 项目名称：云湃工业一体机 · Agent 编排重构 v2
- 项目 ID：yunpai-agent-v2
- 项目负责人：zzg（Agent 执行）
- 风险等级：T2（重写核心编排层，影响全链业务）
- 当前阶段：V2-M3 注册批次进行中（docs/07 执行方案与《注册审查台账》已随 M6 追加至 139 行）；**并行开工 M6 财务**（分支 `feat/m6-finance-20260913`，见 D-005/D-006/D-007/D-008）
- 当前状态：进行中
- 最后更新：2026-09-13

## 项目目标

- 从零重建编排层：LangGraph StateGraph 为**唯一执行路径**（废弃手写循环），设统筹 Agent（按工作流调度）、Worker Agent（执行）、审查 Agent（审查+Gate）三角色。
- 工具/技能先迁移、后逐个审查注册（五项 checklist + 台账）。
- 数据库（8 个 SQLite）与知识库、识别链、自进化包沿用旧仓库实现迁移；自进化补全三缺口（使用反馈接线、知识注入真实消费、红线周期检查）。
- 对外 API 与现有前端契约兼容（/runs、/runs/stream NDJSON、resume、上传）。
- 验收线：W913 订单 workflow 路径 m5.released **且** free 路径全链跑通（修复旧实现 `ingest_m5_planning_snapshot` 装配死点）。

## 范围

### 包含

- 编排层：路由决策、计划生成、工作流引擎（DAG 调度）、统一装配器、审查规则、Gate/resume、流式 API。
- 注册中心：115 个 manifest 工具合同 + 4 个本地识别工具 + 8 个技能的注册与绑定状态表。
- 资产迁移：contracts/registry/存储层/识别四件套/LLM 客户端/evolution 包/测试。
- 自进化接线与三缺口补全。

### 非目标

- 不重写工具业务实现（M0-M5 handler 业务语义不变）。
- 不改前端（他人开发，契约零改动）。
- 不改数据库 schema（8 库表结构沿用）。
- 不做 M7 法务/M8 非标等新业务功能（其工具合入 main 后走注册流程进 v2）。
- **M6 财务口径变更（2026-09-13，D-005~D-008）**：原"不做 M6"改为**在 v2 直接开发 M6**（F-008，分支 `feat/m6-finance-20260913`）；M6 写操作按**三段式（先批准、再落库）**，存量 M0-M5 写工具转 B-014 分批改造。

## 技术路线与关键约束

- FastAPI + LangGraph（StateGraph + interrupt() + SqliteSaver checkpointer）。
- LLM：本地 Qwen（GB10 35B 生产默认 / 本机 27B 联调，OpenAI-compatible 单一配置源）；DeepSeek 仅限授权的进化任务（沿用旧仓库 D-012）。
- 资产复制自 `yunpai-39092-repo@9c7c799`（github/main）+ 主工作区未提交的 evolution 包（与 `github/feat/evolution-20260908` 同源）。
- 设计原则：唯一执行路径 / 装配单源 / 合同驱动审查 / 语义自由非事实自由 / 路由目录只含已绑定工具 / API 契约兼容。

## 数据与安全边界

- 数据分类：业务数据（订单/BOM/SOP/工程图/库存）仅存 runtime SQLite 与受控目录；运行库/演进库/候选库分离。
- 敏感信息（身份证/银行卡/工资）脱敏后才入库；原始敏感文件不进任何 git。
- 大文件（*.sqlite、*.tar.gz、*.zip、*.log、备份目录）不进 git。

## 当前焦点

- 下一里程碑：V2-M3 注册批次 R1-R7（A-003：台账 129 行 100% 登记，五项 checklist 全过）。
- 当前工作重点：**M6 财务（F-008）**——B0a/B0b/R0/R1a 与 B1 前两批（存储层 + 门型、成本账 6 工具 + `_apply_m6_*` commit 钩子）已完成；下一步 B1 其余内核件（`get_product_cost`/`audit_order_cost`/`allocate_expenses`）→ B2 报价单台账。
- 主要阻塞：无（B-001 已裁定关闭；B-013 R0 已决策并执行）。

## 路线变更记录

| 决定 ID | 日期 | 决定摘要 | 原因与影响 | 证据 ID | 确认来源 |
|---|---|---|---|---|---|
| D-001 | 2026-09-09 | 从零重构编排层，新建独立仓库；旧仓库资产以复制方式迁移（基线 9c7c799 + evolution 工作区文件） | 旧 Agent 路由实锤缺陷：LangGraph 空壳、三套路由叠加、free 路径装配缺口（W913 实测死点）；详见 docs/01 §3 | 无 | 用户会话指示（2026-09-09） |
| D-002 | 2026-09-09 | 对外 API 保持与现有前端契约兼容；本阶段只交付两本方案书，不动代码 | 前端他人开发零改动；代码阶段待方案审核后开工 | E-001 | 用户会话决策（2026-09-09 四问四答） |
| D-003 | 2026-09-09 | 三本方案书冻结 v1.0；仓库迁移至 F:\Zcode\Yunpai-v2 独立运作（彻底脱离原工作区，资产+运行库已复制）；按文档进入完整开发 | 用户指示：确认文档、迁移到新目录、完整开发 | E-001 | 用户会话（/goal 确认） |
| D-004 | 2026-09-09 | 注册批次六批扩为**七批**：增补 R3=M0 余 23（http-only），原 R3-R6 顺延为 R4-R7；台账总数账 129 对平 | 书一 §12.3 与书二 §7.3 批次口径矛盾（106≠129，M0 余 23 无批）；书一 §12.3/§13.1、书二 §7.3/§14 已回写，docs/05 偏差记录 #12 | 无 | 用户会话单选确认（docs/07 §2） |
| D-005 | 2026-09-13 | **裁定 B-001**：写工具采用 **propose→approve→commit 三段式**（先批准、再落库）；M6 原生实现（`trial` 草稿 → `finance` 门 → `_apply_m6_*` 翻正）；**新增 `finance` 门型**；存量 M0–M5 维持执行后开门，转 **B-014** 分批改造 | 财务模块不可接受"成本已标正式/月账已冻结后才等人批"；v2 唯一开门点 `graph.py:317` 在 `worker_execute` **之后**（`:362` 才 interrupt）。**先改书再改码**：书二 §6.2 增三段式条文（已补） | E-008 | 用户会话裁定（2026-09-13） |
| D-006 | 2026-09-13 | **R0**：费率/损耗率/库存成本价落 `m6_defaults.py` **口径层**（整块搬老仓 83 行 `m6_defaults.py` + 601 行 `m6_cost.py`）；三层分离（事实永不默认／口径带 `assumed` 痕／缺失 `cost_incomplete`）；`PENDING_FINANCE_CONFIRMATION` 5 条入账本 | 三项输入在 v2 无 canonical 来源；原 M6 计划误标为"M0 canonical ✅ 已有"（事实层与口径层混标）。**比 R1 更早卡**：R1 只卡缺料行，费率缺失卡每一行 | E-008 | 用户会话裁定（2026-09-13） |
| D-007 | 2026-09-13 | **R1 拆分**：R1a 接 M4 采购单只读价源（经 `orchestration_bridge` 装配 `purchase_price_by_material`，M6 **不自开 M4Store**，守 D9）；**R1b**（`supplier_quote` 新实体 + 定写入方）降级为后续项 | v2 已有 M4 采购单读面（`list_m4_purchase_orders` / `get_m4_purchase_order` → `M4Store.list_purchase_orders` / `get_purchase_order`），R1a 只差接一条读口，B1 因此不必等新实体 | E-008 | 用户会话裁定（2026-09-13） |
| D-008 | 2026-09-13 | **「试算快照无门」的契约表达**：`save_costing_snapshot` **显式**声明 `side_effect=local_write` + `review_gate="none"`（不谎报副作用、也不强加门）；**接受 `check_contracts` 的一条 W2**，以此表达「写库但不要门」；改动共享检查器（去 W2）与否决隐式豁免一律不做 | 试算只写 `status=trial`、**不进月末汇总**（`m6_store.month_summary` 只认 `confirmed`），不构成生效事实；而 v2 契约按 `side_effect` 推导默认门（`registry.py:62`）→「写库且无门」原本表达不出来（计划 §1.2 记为模型缺口）。计划 §6 的核心可用性论点正是"不能每笔成本都人工确认"，试算快照量最大。基线已有同性质先例（`ingest_recognized` 亦为显式 `review_gate=none` 的写工具）；该 W2 由测试锁定为**刻意**而非漂移 | E-008 | 用户会话（2026-09-13，交接 §7.2 授权：说明为什么接受一条 W2 或改用别的表达）|
