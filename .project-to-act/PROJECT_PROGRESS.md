# 项目进度

> 只记录当前执行状态和少量近期节点；详细工作日志保存在项目正常材料中。

## 当前任务

| 任务 ID | 任务 | 状态 | 负责人 | 完成条件 | 证据 ID | 最后更新 |
|---|---|---|---|---|---|---|
| P-001 | 三本方案书撰写与审核入库 | 已完成 | Agent | 用户确认冻结 v1.0 | E-001 | 2026-09-09 |
| P-002 | V2-M1/M2：骨架 + 资产迁移 + 测试跑绿 | 已完成 | Agent | pytest 376 passed/4 skipped + API 冒烟全通 | E-002 | 2026-09-09 |
| P-003 | V2-M3：119 工具+8 技能逐个审查注册（R1-R7，七批制 D-004） | 已完成 | Agent | 台账 100% 登记，五项 checklist 全过 | E-004 | 2026-09-09 |
| P-010 | V2 工具迁移集成收口（6 分片合并 + fact_gateway + W913 双路径 + 账本） | 已完成 | Agent | 全量 pytest 642 passed/4 skipped + check_contracts exit 0 + 账本收口 | E-004 | 2026-09-09 |
| P-011 | Gate 覆盖缺口微收口（R21：`data_import_commit` 声明对齐 + 断言） | 已完成 | Agent | 全量 pytest 643 passed/4 skipped + check_contracts exit 0 + 缺口 0 | E-005 | 2026-09-09 |
| P-012 | W913 workflow 路径打通（INT2 第四轮 · K1–K6：M0 前向装配 / 门补数可见 / 工序形状 / 降级日历 / 降级资源） | 已完成 | Agent | workflow 路径 `completed` + `m5.released` + 判定器 PASS 5/5；全量 pytest 659 passed/4 skipped + check_contracts exit 0 | E-006 | 2026-09-09 |
| P-013 | 基础资料上传接线（INT2 第五轮 · G1–G4：master_data 路由 / 技能载荷 / 候选门+canonical 发布 / 缺产品编码 fail-closed） | 已完成 | Agent | 面板形状实跑 `route=free`+`published 72`（bom W-H913/document W-H913-sop），再传订单 M2 **无 data 门**（bom 248 行/SOP 15 工序）；全量 pytest 682 passed/4 skipped + check_contracts exit 0 | E-007 | 2026-09-09 |

## 阻塞项

| 阻塞 ID | 阻塞 | 影响 | 解除条件 | 状态 |
|---|---|---|---|---|
| B-001 | **R8 已知缺口（P0）：`local_write`/`external_write` 工具的写在人工批准之前已发生**（M0 6 类、M1 4 件、M3 4 件远端审批、M4 10 件、M5 5 件，逐条见各分片报告「已知缺口」表）。V2 门模型只有执行后审查（`graph.py` 唯一开门点），无执行前 authorization 等价物 | 授权门语义与书二 §6.2「先授权后执行」不一致；写已落库才开门 | **先改书再改码**：书二 §6.2 明确 propose→approve→commit 三段式（草稿/候选先落，批准后提交），再按书改码 | 待裁决 |
| B-002 | Gate 覆盖核对缺口 1（**已修复**）：`data_import_commit` RULES 仅 `fail`、无 `gate:` 动作，manifest 曾声明 `review_gate=candidate`（`m0.json:149-150`）但图内零消费（`default_gate_for_authorized` 死代码），且无 candidate 断言 | 已消除：声明与生效门型一致（`review_gate=data` → `gate_type_for` = `blocked_input`，与 `evaluate()` 实际路径对齐） | **已修复（R21，2026-09-09）**：`m0.json:150` 改 `review_gate=data`（保留 `side_effect=external_write`）+ 断言 `tests/test_migration_m0.py:466-507`；RULES **有意不加** `gate:` 动作（发布人工门在 `data_import_run`，不重复开门） | 已修复 |
| B-003 | **K1**：raw 订单 xlsx 进 M0 被隔离（`quarantined:[no_structured_records]`）→ `data_import_run` `status=failed` **且不开门**，人工无法恢复；V2 缺 M1 解析结果 → M0 结构化订单的前向改写函数 | 已消除：raw 订单 → `order-canonical.json` 结构化 records → M0 建候选 → candidate 门 → commit | **已修复（第四轮 K1，2026-09-09）**：`orchestration_bridge.py:1086-1128` 新增 `forward_m1_order_canonical`（逐行语义等价迁自 `_wt/REALFLOW/.../orchestration_bridge.py:872-912`）+ `:1165-1195` 在 `data_import_run` 分支调用；断言 `tests/test_int2_r4_w913_bridge_fixes.py` 4 条（含 `CanonicalImportStore.register_batch` 零隔离实证） | 已修复 |
| B-004 | **K2/K3**：门里补的数据装配看不见——`reviewer/gates.py` 把 supplement 写进 `request[tool]`，而装配读**顶层** `request["inventory_snapshot"]`（`orchestration_bridge.py:872-882`）/`request["supplier_by_material"]`（`:917`）→ M3/M4 `data` 门 `retry+supplement` 连续 3 次无效 | 已消除：M3 库存门、M4 供应商门补数一次生效 | **已修复（第四轮 K2/K3，2026-09-09）**：`reviewer/gates.py:121-136` supplement **双写** `request[tool]`（工具载荷兼容）与顶层 `request`（同键覆盖，`None` 不写入、不覆盖已有事实）；断言 3 条（含 `bridge_payload` 两个工具都拿到值） | 已修复 |
| B-005 | **K4**：`solve_scheduling` 合同校验失败——`routing_steps[].eligible_resources` `minItems=1`，而 `orchestration_bridge.py:1488-1512` 只在步骤带 `required_*_codes`/`eligible_resources` 时才能构造；SOP 工序只带工位名（如「排卡」）时为空数组 | 已消除：工位名兜底 + 分钟口径归一（秒/字符串显式换算），亚分钟工时兜底 1 分钟 | **已修复（第四轮 K4，2026-09-09）**：`orchestration_bridge.py:590-608` 新增 `_standard_minutes_of`（分钟口径，`standard_time*` 按秒 /60）+ `:626-631` 归一化接线 + `:1493-1512` 工位名兜底；断言 3 条 | 已修复 |
| B-006 | **K5**：`NO_FEASIBLE_WINDOW op_code=TX-001-02`——降级日历硬编码「今天起 7 天 08:00–17:00」（`orchestration_bridge.py:523-544`），订单交期 2026-07-10 已过 → 无可用窗口 | 已消除：降级日历按**排产地平线**生成（`start=min(今天,交期)-1天`、`end=max(今天,交期)+30天`），交期在过去也被覆盖；未给交期仍退化为原「未来 7 天」 | **已修复（第四轮 K5，2026-09-09）**：`orchestration_bridge.py:523-588`（新增 `_planning_horizon_date` + 重写 `_degraded_calendar_snapshot`）；断言 2 条；实跑 93 个窗口 `2026-07-09 → 2026-10-09` | 已修复 |
| B-007 | **K6**（父会话同轮预判）：降级资源只补**新建**对象——`_degrade_resource_snapshot` 给新工位补 `calendar_ref`（原 `:609`），但**已存在**的 stations/persons/tooling 直接透传（原 `:595`）→ canonical SOP 派生工位缺 `calendar_ref` 时 M5 严格校验报 `MISSING_VALUE resource_snapshot[...] stations calendar_ref`，且 `read_m5_resource_facts` 优先于人工补数 | 已消除：已存在的工位/人员/模治具与设备同口径兜底（缺什么补什么，保留 `degraded` 标记，不覆盖已有事实） | **已修复（第四轮 K6，2026-09-09）**：`orchestration_bridge.py:635-762`；断言 3 条（含 `validate_resource_snapshot(finalize_snapshot(...))` 严格校验实证） | 已修复 |
| B-008 | **G1**：只含 `kind=master_data` 附件（BOM/SOP）的请求被判 `workflow/m1_m5_document_to_plan` → 永远到不了 `business-data-identification` 技能，基础资料落不了 canonical。深根因二：① `llm.py` system prompt 两句判据自相矛盾（BOM/SOP 同时命中「上传原始业务文件→workflow」与「上传基础资料→skills」，实测 LLM `raw` 同时给出 `workflow_id` 与 `skills`）；② `router._validate` 的 `route=workflow` 分支**静默丢弃** LLM 提案的 skills | 已消除：全 master_data 附件（无 order）→ 确定性 `route=free`+`skills=[business-data-identification]`；矛盾 LLM 提案不让生效（交确定性规则接管）；prompt 判据互斥化；`_validate` 保留 skills 并在 reason 说明「不派发、仅留痕」 | **已修复（第五轮 G1，2026-09-09）**：`orchestrator/router.py:73-91`（`_contradicts_master_data` 否决）、`:163-185`（规则 0）、`:229-246`（`_validate` 保留 skills + reason）、`:250-283`（`_attachment_items`/`_all_master_data`）；`llm.py:108-112`（判据互斥 + 技能内部工具不重复列）；断言 7 条（`tests/test_int2_r5_master_data_upload.py` G1 段） | 已修复 |
| B-009 | **G2**：技能只拿到 `request[技能名]`（`worker/executor.py:68`），前端面板把 `message`/`documents` 放顶层 → 产品编码正则取不到、文件也看不到（实测 `records=0`） | 已消除：派发 skill 时把顶层 `message`/`product_code`/`product_name`/`attachments`/`documents` **仅缺键时**并入载荷（显式值优先），并补 `documents→files` 别名（legacy `_payload_for` 同口径） | **已修复（第五轮 G2，2026-09-09）**：`worker/executor.py:54-93` 新增 `skill_payload` + `:119-121` 接线；断言 3 条 | 已修复 |
| B-010 | **G3**：技能返回 `m0_candidate_records`（实测 71/72 条）却**不开门、不发布**——`reviewer/rules.py` 无该技能条目、`graph.py` 无该分支发布钩子 → 候选静默丢弃 | 已消除：候选非空 → `candidate` 门；批准即 `CatalogService.publish_records` 发布 canonical 并写回 `m0_catalog_publish`（发布失败 fail-closed 重开门，不伪造计数） | **已修复（第五轮 G3，2026-09-09）**：`reviewer/rules.py:128-146`（RULES 条目）；`graph.py:269-345` 新增 `_publish_business_canonical` + `:483-503` approve 分支接线（`review_applied` + trace `m0.canonical_publish`）；断言 7 条（含 LangGraph 端到端） | 已修复 |
| B-011 | **G4**：产品编码缺失时静默 `records=0` + `run=completed`，操作者不知道缺什么 | 已消除：`status=needs_product_code` + `missing_fields=["product_code"]` + 可读 message → 审查门开 `blocked_input`（补 product_code 重试），不发布空批次 | **已修复（第五轮 G4，2026-09-09）**：`skills.py:201-227`（fail-closed 分支）；断言 3 条；`tests/test_uploads.py` 既有用例补显式 `product_code`（原断言在 G4 语义下不再成立） | 已修复 |
| B-012 | **R2 读口缺第 2 层归一化**（第五轮验收步骤 4 实测）：`fact_gateway.read_entities` 只把**信封层** `attributes` 并入顶层，未处理 `m0.ingest.v1` 业务体 `payload.attributes` → SOP 的 `route_steps` 读不到（实测 `route_steps=0`，M2 工程事实不完整；BOM 的 `lines` 在业务体顶层故未暴露） | 已消除：读口对业务体再归一化一次（只增不改、同名顶层优先），SOP 工序可读（实测 15 道） | **已修复（第五轮 R2 补，2026-09-09）**：`fact_gateway.py:97-150`；断言 1 条（`test_g3_r2_read_entities_merges_business_body_attributes`） | 已修复 |
| - | 无 | - | - | - |

## 下一步

1. feat/orchestrator-skeleton-20260909 收口合 main → 拉 feat/orchestrator-register-<开工日>。
2. 按 docs/07 §4 工作卡开工 R1（识别 4+M0 沙箱 5；建 test_ledger_consistency + test_bridge_rules_coverage；**E-003 四项发现随 R1 修补**：/tools 暴露与绑定表矛盾、非法参数 500、LLM workflow 提案闭环缺失、Qwen 超时/占位 key 配置）。
3. 集成分支 `feat/migration-integration-20260909` 推送即更新 **PR #2**（不另开）；B-001（批准前已写，P0）按「先改书再改码」立项。
4. W913 双路径已通（P-012/E-006）→ 待办：B-001 三段式（先改书再改码）、R13 供应商权威源、R19 M0 HTTP 路由、R11 M4 文档漂移；K6 的「M0 canonical 工位缺 `calendar_ref`」实跑路径待有真实 canonical 工位数据时复验。
5. 基础资料上传已通（P-013/E-007）→ 待办：canonical SOP 无权威工时（`standard_minutes`/`required_equipment_codes`）→ M5 快照门缺数，需业务补数据或单独立项；库存/工程图未纳入 `canonical_records_from_batch`（后续扩展）；V2 未接线 `merge_m2_canonical_bom`（无 `canonical_bom_match`/`canonical_sop_match` 字段，后续项）。

## 进度历史

| 日期 | 会话或任务 ID | 工作内容摘要 | 关键确认或纠正 | 证据 ID | 遗留问题 | 下一步 |
|---|---|---|---|---|---|---|
| 2026-09-09 | P-001 | 仓库建立、账本初始化；全量探查旧仓库（文档/编排/资产三路）；开始撰写 docs/01、docs/02 | 用户四项决策：独立仓库/基线 main+资产复制/只交付两本书/API 契约兼容 | 待补 | 书二 115 工具注册清单需按 manifest 实名填写（已提取） | 完成两本书并提交审核 |
| 2026-09-09 | P-001 | 书一/书二完成入库（4 commit）；按用户反馈新增书三《功能需求对齐手册》（docs/03，45+ 功能卡全部挂需求文档出处：01 产品功能/03 数据需求/04 工具接口/11 白板蓝图/自进化设计稿/09-10 原始需求）；任务表 xlsx 改号 04 | 用户反馈：书二勿动；新书要"每个功能具体是什么、为了实现什么"，须多参照需求文档（需求文档大部分没问题） | 待补 | 书三待用户逐卡审核 | 三本齐后冻结 v1.0 基线 → V2-M1 |
| 2026-09-09 | P-002 | 仓库迁至 F:/Zcode/Yunpai-v2（脱离原工作区，运行库连数据复制）；三书冻结 v1.0（D-003）；分支 feat/orchestrator-skeleton-20260909：资产迁移（115 合同+进化包+存储识别链+handler+随迁测试+local.json 识别合同）→ 新骨架（config/state/build_graph 三 Agent/装配器收编 bridge/审查规则表/绑定状态表/API 契约兼容/专属循环执行模型/Python3.12 venv）→ pytest 376 绿 + API 冒烟全通 | 实现偏差 11 条入档 docs/05；旧图强耦合测试 14 个归档 legacy_rewrite；langgraph@3.10 interrupt 坑与 AsyncSqliteSaver 循环绑定坑实测定型 | E-002 | workflow 全链（W913）与逐字段流式快照在 M5；identity 端点与 readback 在 M3 | P-003 注册批次（R1 识别+M0 起） |
| 2026-09-09 | P-002 | 生成交接包 F:/Zcode/Yunpai-v2-交接包-20260909（git bundle 全历史+源码 zip+5 库快照+三书账本+验证证据 pytest376/冒烟+HANDOFF）；HANDOFF 副本入库 docs/06-交接-v2.md | 应用户要求 | E-002 | - | P-003 注册批次 |
| 2026-09-09 | P-003 | 读取交接包续建开发方案：docs/07《V2-M3 注册批次开发方案》（事实基线实测+R1-R6 工作卡+批次修订 D-004 建议+防漂移机制 test_ledger_consistency）+《注册审查台账》初值入库（129 行全实名=119 工具+8 技能+2 反馈面，绑定/装配/审查列程序化导出自代码实况）；实测挖出 5 个审查疑点（m3 审批 blocked_input 门错位、审批 4 件缺默认隐藏机制、import_m4_purchase_suggestions 未绑定、M0/M2 29 件 http-only 口径、replan/dispatch 缺 plan_version 桥） | 书一 §12.3 与书二 §7.3 批次总数账矛盾（106≠129，M0 余 23 无批）由 docs/07 §2 裁定为 D-004 建议，待用户确认后回写两书 | 待补 | 生成脚本已删（防误跑覆盖台账）；identity 端点归 R2a、evolution/shortcuts 归 M4 | 用户确认 D-004 → skeleton 合 main → R1 开工 |
| 2026-09-09 | P-003 | 开工门基线复验（用户指令，含 DeepSeek 真实 LLM 测试）：.venv(Py3.12) pytest 376 绿对齐 E-002；注册目录静态对账全对上（119=115+4、四态 30/53+1/35、红线无泄漏、production 降级、8 技能）；路由三层链验证=专项单测 18/18 + 活体冒烟 9 用例×2 配置（确定性 + DeepSeek 真实 LLM 经 QwenRouter 同码路径），LLM 提案 chat/free 全链走通、护栏与 CatalogView 过滤经真实模型验证有效；真实后端对比 DeepSeek 1-3s vs 本地 Qwen 49-76s | DeepSeek 仅为测试替身注入测试进程 env（红线：生产识别/路由仍只用本地 Qwen，未改任何生产配置/代码）；4 项发现（/tools 暴露与 bound 矛盾、非法参数 500、LLM workflow 提案闭环缺失、本地 Qwen 超时>45s 默认值）详见 E-003 | E-003 | 默认 python=3.10 与 .venv 并存易误用（6 failed 已归因为环境非回归） | 用户确认 D-004 → skeleton 合 main → R1 开工（4 项发现并入 R1 修补清单） |
| 2026-09-09 | P-003 | D-004 批次裁定落地（用户单选确认）：注册批次六批扩为七批——M0 余 23 单独成新 R3，原 R3-R6 顺延为 R4-R7（9+24+23+20+26+17+10=129 对平）；回写书一 §12.3/§13.1、书二 §7.3/§14、docs/05 偏差记录 #12；docs/07 全篇与《注册审查台账》129 行按 R1-R7 重排；E-003 四项发现并入 docs/07 R1 工作卡 | 用户选择"M0 余 23 单独成批"（三选一，AskUserQuestion） | 无 | - | skeleton 合 main → R1 开工（含 E-003 四项修补） |
| 2026-09-09 | P-003 | LLM workflow 提案闭环修复 + WF 全链闭环验证（用户指令）：①按书二 §4.1 补齐路由 prompt 工作流目录（llm.py 三处+7 测试）；②E2E 依次挖出并修复 stale-resume 卡死（非法决策改再挂起 gate_invalid）、工程门 approval_status 落点缺失、checkpoint/runs 同库写锁竞争（checkpoint 分库+镜像重试退避）、多行订单 m5 快照批内 UNIQUE 冲突（行号按序唯一）——共 +4 测试，pytest 387 绿；③终局 E2E：DeepSeek 真实 LLM 提案 workflow=m1_m5_document_to_plan → 上传带码订单（库存/供应商/资源/日历/路线/供应六类权威事实顶层喂入）→ **9/9 步 completed**，5 次 Gate 审批（review×2/候选裁决/工程/apply），排程 plan-scenario-WX20260905001-d80bab1814 回读成功，response=「业务链执行完成：9/9 步，Gate 审批 5 次」 | 六类快照的权威事实要求逐项实测：缺一即确定性阻断并给精确补数指引（「语义自由非事实自由」红线经真实链路验证）；新遗留观察：TOOL_ERROR 步骤会被审查默认放行标 completed → 已修复（见偏差 #15）；偏差 #13/#14 入档 docs/05 | 待补（pytest 387 绿 + E2E run-95a494af，临时脚本在 %TEMP%\yunpai-verify） | - | skeleton 合 main → R1 开工 |
| 2026-09-09 | P-003 | 链路功能/逻辑筛查（用户指令，后续工作地基）：读全 planner/workflow_engine/executor/state/repository/graph/rules/contracts 主干，实测三处「假成功」并修复——①工具异常被审查静默标 completed（复现：无规则工具抛异常 → run=completed）→ failed 步骤按重试余量重试、耗尽即 failed，executor 对非阻塞 success=False 也标 failed；②finalize 对 blocked/pending/running 残留误判 completed → failed>残留>completed；③LLM free 空工具+空技能空转「0 步完成」→ _validate 拒绝。共 +3 测试，pytest **390 绿**；偏差 #15 入档 docs/05 | 另两项记录不修（待定）：① spec.review_gate 合同默认门是死代码（RULES 表外工具的合同门不触发，default_gate_for_authorized 零调用，属 docs/05 #9 注册批次 R2-R5 接线）；② 行标识不一致——M1 line_id={order}::{sheet}!R{row} 与桥接 order_line_id={order}::L{index} 从未打通，真实多源数据会错配，需统一约定后透传 | 待补（pytest 390 绿） | - | skeleton 合 main → R1 开工（行标识约定 + 默认门接线并入 R1） |
| 2026-09-09 | P-004 | M0 分片迁移施工（worktree `_v2work/M0`，分支 `feat/migration-m0-20260909`）：随迁 m0_facts/m0_import_store/m0_catalog_ingest/m0_catalog_templates + 补 m0_backend 候选裁决三方法；workers 注册 23 个 handler（31→54）；bridge 补 C3 装配与 C4 进程内读；rules 补 17 条 M0 门 + Gate 可诊断字段；graph 补 candidate 门批准落地裁决（解 W913 死锁）；契约对齐 R1（18 处）与 rows-S0 四件套；+27 用例 | 证据：`_migration/REPORT-MIG-M0.md`、`REQUESTS-MIG-M0.md`；提交 df16081..85e131d；pytest 423 passed/4 skipped（exit 0）；check_contracts exit 0 | 遗留：M0 HTTP 22 条路由、ingest_recognized W2、resolve 契约 kind/id 漂移、写工具批准前已写（设计偏差） | 确认来源：父会话裁定（R1/R2 读口、RULES+manifest 双写、R8 不恢复执行前门） |
| 2026-09-09 | P-005 | M1 分片迁移施工（worktree `_v2work/M1`，分支 `feat/migration-m1-20260909`）：17/17 全部「改造后搬」——workers 落地 16 个新 handler + 覆盖 ingest_document 的 fixture 版；随迁 m1_knowledge.py（5 个知识链 handler）并改走 `M0Store.list_entities` 读口；4 条写工具补 `gate:authorization` + manifest 双写；+27 用例 | 证据：`_migration/REPORT-MIG-M1.md`；提交 0b5ade5/44afaf1/ed91b05/4da7816；pytest 423 passed/4 skipped（exit 0） | 遗留：m0_catalog_relations 表在 V2 无创建者（REQUESTS-MIG-M1 RQ-M1-3）；写工具批准前已写 | 确认来源：父会话 R3 裁定（m1_knowledge 随迁）+ R8 |
| 2026-09-09 | P-006 | M2 分片迁移施工（worktree `_v2work/M2`，分支 `feat/migration-m2-20260909`）：7/7 已迁（6 直接搬 + 1 改造后搬 `search_m2_bom_history` 改走 M0 读口、读口异常写 warnings）；补 3 条写工具 authorization 门、2 条缺口测试 + 33 条 M2 回归 | 证据：`_migration/REPORT-MIG-M2.md`；提交 b1ecf58..76a81c1；pytest 429 passed/4 skipped（exit 0） | 遗留：写工具批准前已写 | 确认来源：父会话 R1/R2 读口裁决 |
| 2026-09-09 | P-007 | M3 分片迁移施工（worktree `_v2work/M3`，分支 `feat/migration-m3-20260909`）：17 件（改造后搬 2：`get_material_readiness_snapshot`/`run_m3_procurement_requirements`；保留 HTTP 13 + 补绑定与审查断言；不搬 1 `receive_m3_material_demand`；废弃 1）；契约同步 9 处、审批四件门修正、路由目录默认隐藏 4 件；+65 用例 | 证据：`_migration/REPORT-MIG-M3.md`；提交 791fc13…8ca558c；pytest 460 passed/1 failed（仅 `test_migration_infra` 骨架断言，父会话 INT2@53e1357 已修）/4 skipped；check_contracts exit 0 | 遗留：`gate_type_for` 未区分合同门/结果门（R14 登记）；写工具批准前已写 | 确认来源：父会话 R14 裁定 |
| 2026-09-09 | P-008 | M4 分片迁移施工（worktree `_v2work/M4`，分支 `feat/migration-m4-20260909`）：26 工具 = 已迁 24（直接搬 23 + 改造后搬 1 `import_m4_purchase_suggestions_json` 真实实现）/ 废弃 1 / 不搬 1；16 条 RULES + manifest `side_effect`/`review_gate` 双写；P0 三项修复（假实现替换、`send_m4_purchase_order` 远程禁令升级为 registry 强制、`query_m4_material_supply_snapshot` 惰性写挂门） | 证据：`_migration/REPORT-MIG-M4.md`、`tests/test_m4_review_gates.py`（47 例） | 遗留：M4 文档漂移（skills/m4/references/tools.md，R11 本轮不动）；供应商权威源双写风险（R13 待裁决）；写工具批准前已写 | 确认来源：父会话 R11/R12/R13/R17 裁定 |
| 2026-09-09 | P-009 | M5 分片迁移施工（worktree `_v2work/M5`，分支 `feat/migration-m5-20260909`，5 个提交）：20 工具（直接搬 5 / 改造后搬 13 / 不搬 2）；m5_tools 迁入 R6 8 处语义 + 阻塞信封统一顶层 `code`（2 处合同强制例外保留异常）；rules 新增 4 条写工具门（replan→apply、dispatch/prepare/record→authorization）；m5.json 6 写工具双写 + 10 处描述/schema 同步；graph 新增 `_apply_m5_release`（apply 门 head CAS 真实发布 + 冲突重开门）；+21 用例 | 证据：`_migration/REPORT-MIG-M5.md`；pytest 417 passed/4 skipped（exit 0）；check_contracts exit 0 | 遗留：②例外 2 条需契约裁定（REQUESTS-M5 #1）；写工具批准前已写；`ingest_m5_planning_snapshot` 按合同 `review_gate=data` 兜底不加后置门 | 确认来源：父会话 R8/R9/R20 裁定 |
| 2026-09-09 | P-010 | **集成收口（INT2）**：6 分片全合入 `feat/migration-integration-20260909`（M2 e94984d → M3 4f01478 → M4 aeb0254 → M5 fbffd9b → M0 9e5440f → M1 394e9d5）+ R16 跨切面修复 8759a0d + 共享断言收口 c1cc540 + `fact_gateway.py` 抽取（R2 单点化，替换 5 处局部归一）；W913 双路径实跑（free 路径 `ingest_m5_planning_snapshot` completed/`snapshots_stored`，workflow 路径实测至 M5 solve）；Gate 覆盖系统性核对 44 需补 → 42 已覆盖 / 1 缺口；账本收口（F-004/F-006/P-004..P-010/E-004 + B-001/B-002） | 证据：`_migration/REPORT-MIG-INTEGRATION.md`、`_migration/exec/GATE-COVERAGE-INT2.md`；全量 pytest **642 passed/4 skipped**（exit 0）；check_contracts exit 0；面指标 handlers 114/bound_local 100/unbound 6/visible 109/rules 51 | 遗留：workflow 路径未达 `m5.released`（4 类卡点见报告 §4）；R8 批准前已写（B-001）；`data_import_commit` 门漂移（B-002）；M4 文档漂移（R11） | 下一步：父会话 push/PR + 按 B-001/B-002「先改书再改码」立项 |
| 2026-09-09 | P-011 | **Gate 缺口微收口（INT2 第三轮 · R21）**：`data_import_commit` 声明漂移收口——`registry-manifests/m0.json:150` `review_gate: candidate → data`（`gate_type_for` 实测 → `blocked_input`，R20），`side_effect=external_write` 保留；RULES `rules.py:83-85` **有意不加** `gate:` 动作（发布人工门已在上游 `data_import_run`，不重复开门）；补断言 `tests/test_migration_m0.py:466-507`（成功不开门 / `BLOCKED_INPUT`→`blocked_input` 且带 `message`+`missing_fields` / `gate_type_for("data_import_run")=="candidate"` 且 commit ≠ `candidate`）；未改 `gate_type_for`/`default_gate_for_authorized`、未动 K1–K5、未开新 PR | 口径偏离获批（父会话裁定）：不写 `review_gate=none`（会新增 W2 噪音：写工具无门）、不删字段（`registry._contract_defaults` 回退 `gate_by_module["m0"]="candidate"` → 漂移静默复原）、选 `data`（与 M5 `ingest_m5_planning_snapshot` 先例对称） | 证据：`_migration/REPORT-MIG-INTEGRATION.md` §5/§9/§10、`_migration/exec/GATE-COVERAGE-INT2.md` §3.3「收口记录」；全量 pytest **643 passed/4 skipped**（exit 0）；`check_contracts.py` exit 0（`--strict` 仍只余存量 W2 `ingest_recognized` 1 条） | 遗留：workflow 路径未达 `m5.released`（K1–K5）；B-001 批准前已写 | 下一步：推送更新 PR #2 + 按 B-001 立项 |
| 2026-09-09 | P-012 | **W913 workflow 路径打通（INT2 第四轮 · K1–K6）**：K1 补 `forward_m1_order_canonical`（M1 解析行项 → `order-canonical.json`，`orchestration_bridge.py:1086-1128` + `:1056-1061` 接线）；K2/K3 `reviewer/gates.py:121-136` supplement 双写 `request[tool]` + 顶层 `request`（`None` 不写、不覆盖已有事实）；K4 `_standard_minutes_of` 分钟口径归一（`:590-608`、`:626-631`）+ 工位名兜底（`:1493-1512`）；K5 降级日历按排产地平线生成（`:523-588`）；K6 已存在 stations/persons/tooling 同口径兜底（`:635-762`）；新增 `tests/test_int2_r4_w913_bridge_fixes.py` 16 条 | 验收口径：workflow 路径 **completed + `m5.released`**（`run-b84071dc16684aa0831880b3e40607b0`，route.source=explicit，`plan_version=plan-scenario-PO-20260625-003-9315b2d6ee`，`is_current_head=true`）；free 路径未退化且真 LLM 路由（`run-3f6b938372344669931595d28d24cf4c`，route.source=**llm** confidence 0.95，同 `released`）；无 LLM 环境下的 free 回退路由亦 `run-a4b14bbc490d4fc3aba5ef453982f66d` completed+released；三条 run 判定器均 PASS 5/5；makespan 3220 / processing 1420 分钟（非零工时） | 证据：`_migration/REPORT-MIG-INTEGRATION.md` §11；`_localrun/sim/v2-w913-r4-workflow.json`、`v2-w913-r4-free.json`、`v2-w913-r4-evidence.json`（+ `*-run.json` 全量 state）；全量 pytest **659 passed/4 skipped**（exit 0）；`check_contracts.py` exit 0（`--strict` 仍只余存量 W2） | 遗留：B-001（R8 批准前已写）仍待「先改书再改码」；K6 实跑路径的工位来自 supplement（自带 `calendar_ref`），K6 由严格校验单测锁定 | 下一步：推送更新 PR #2（不另开） |
| 2026-09-09 | P-013 | **基础资料上传接线（INT2 第五轮 · G1–G4）**：G1 `orchestrator/router.py` 新增确定性规则 0（附件全 `kind=master_data` 且无 order → `route=free`+`skills=[business-data-identification]`，放在「有业务文件→workflow」之前）+ 矛盾 LLM 提案否决（`_contradicts_master_data`）+ `_validate` 的 `route=workflow` 不再静默丢 skills（保留并写进 reason）+ `llm.py` prompt 两条判据互斥化（含「选 Skill 时不重复列其内部工具」）；G2 `worker/executor.py` 新增 `skill_payload`（顶层 `message`/`product_code`/`product_name`/`attachments`/`documents` 仅缺键并入，显式优先，`documents→files` 别名）；G3 `reviewer/rules.py` 加技能 RULES 条目（候选非空 → candidate；`needs_product_code` → blocked_input）+ `graph.py` 新增 `_publish_business_canonical`（候选门批准 → `CatalogService.publish_records` 发布 canonical，写回 `m0_catalog_publish`/`review_applied` + trace，发布失败重开门不伪造计数）；G4 `skills.py` 缺产品编码 → `status=needs_product_code`（fail-closed，不发布空批次）；R2 补：`fact_gateway.read_entities` 对业务体 `payload.attributes` 再归一化（SOP `route_steps` 可读）；新增 `tests/test_int2_r5_master_data_upload.py` 23 条 + `tests/test_uploads.py` 既有用例补显式 `product_code` | 验收（面板形状，真 LLM 路由）：`run-6793a0db34ae4b04aeae70ac36b5ff74` route=free/source=llm（conf 0.9）/tools=[]、skill `candidate_created`/`product_code=W-H913`/72 候选 → approve candidate 门（m0-reviewer）→ `m0_catalog_publish.published=72`、canonical `bom W-H913`+`document W-H913-sop`+69 materials+`product W-H913`（`M0Store.list_entities` 佐证）；再传订单 `run-ba604dea327741c0af2db27a451ea022`：**M2 无 data 门**（门序 candidate/data_import_run → engineering/run_bom_sop_workflow → M3 库存门 → M4 供应商门 → M5 快照门），M2 结果 `bom_lines=248`/`route_steps=15`（canonical 命中） | 证据：`_migration/REPORT-MIG-INTEGRATION.md` §12；`_localrun/sim/int2-r5-accept.json`、`int2-r5-master-run.json`、`int2-r5-order-run.json`；全量 pytest **682 passed/4 skipped**（exit 0，基线 659 → +23）；`check_contracts.py` exit 0 | 遗留：B-001（R8 批准前已写）；canonical SOP 无 `standard_minutes`/`required_equipment_codes` → M5 快照门仍缺权威工时（源数据缺口，本轮不扩展）；库存/工程图仍未支持（`canonical_records_from_batch` 仅 bom/sop）；V2 未调用 `merge_m2_canonical_bom`（无 `canonical_bom_match`/`canonical_sop_match` 字段，以 M2 payload/结果计数佐证） | 下一步：推送更新 PR #2（不另开）；上述三项缺口后续单独立项 |
